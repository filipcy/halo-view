# CAD

HALO View mechanical source, review records, and controlled manufacturing exports live here.

## Structure

- `source/` — editable source models and generators
- `reviews/` — iteration review notes and review graphics
- `exports/` — neutral STEP exports
- `stl/` — released print meshes
- `prototypes/` — fit-test and Rev A sections
- `references/` — validated reference geometry

## HALO Dock Rev A

Iteration 2 is generated by [`source/HALO_Dock_Rev_A.py`](source/HALO_Dock_Rev_A.py). It retains the `TabletEnvelope`, split `Faceplate`, `DockBody`, `WallInterface`, and `Assembly` structure, then adds parameter-driven side guides, a lower shelf, non-final upper side-detent studies, and hidden flat mounting fields. The top insertion path stays open. The WallInterface fields physically establish the 1.5 mm shadow gap, and the generator validates the nominal 18 mm wall-to-front stack.

See [`reviews/HALO_Dock_Rev_A_Iteration_2.md`](reviews/HALO_Dock_Rev_A_Iteration_2.md) and its [review schematic](reviews/HALO_Dock_Rev_A_Iteration_2.svg) for scope, exclusions, projection math, and release gates. The [Iteration 1 review](reviews/HALO_Dock_Rev_A_Iteration_1.md) remains the accepted baseline record.

Released files must use controlled part names and revision identifiers. Generator output is a working artifact until its review gates are closed and it is explicitly copied into a release directory.

## Physical fit corrections

The accepted PETG coupon results are recorded in [`reviews/HALO_Dock_Rev_A_Physical_Fit_Validation.md`](reviews/HALO_Dock_Rev_A_Physical_Fit_Validation.md). The generator now selects 0.20 mm per-side tablet clearance and R8.5 corners, strengthens the existing-width lower shelf without changing its fit or height, and applies explicit +0.30 mm downward and +0.20 mm rear USB-C pocket relief. Historical 0.20/0.30/0.40 clearance and R8/R8.5/R9 corner coupon variants remain generated for traceability. Native Fusion rebuild, interference, export, and visual checks remain mandatory.
